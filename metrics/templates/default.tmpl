{{ define "custom_title" }}
   {{ range .Alerts }}
       {{ if eq .Status "firing" }}:broken_heart:{{else}}:green_heart:{{ end }} [{{ .Annotations.identifier }}] {{.Annotations.summary }}
   {{end}}
{{ end }}
{{ define "custom_mail_message" }}
    {{ range .Alerts }}
        {{ if eq .Status "firing" }}
            {{ .Annotations.casDown }}
        {{else}}
            {{ .Annotations.casUp }}
        {{end}}
    {{end}}
{{end}}